tangaza_amakuru(...values) -> ubusa
injiza_amakuru(...prompt) -> number | string | ubusa
sisitemu(command) -> string
ubwoko(value) -> string
filename -> string
nibyo
sibyo
ubusa
ikosa

KIN_AMAGAMBO.huza(...parts) -> string
KIN_AMAGAMBO.ingano(text) -> number
KIN_AMAGAMBO.inyuguti(text, index) -> string
KIN_AMAGAMBO.inyuguti_nkuru(text) -> string
KIN_AMAGAMBO.inyuguti_ntoya(text) -> string
KIN_AMAGAMBO.tandukanya(text, separator) -> array

KIN_URUTONDE.ingano(list) -> number
KIN_URUTONDE.ongera_kumusozo(list, value) -> number
KIN_URUTONDE.siba_kumusozo(list) -> number
KIN_URUTONDE.injiza_ahabanza(list, value) -> array
KIN_URUTONDE.siba_ahabanza(list) -> array
KIN_URUTONDE.ifite_ikirango(list, key) -> bool
KIN_URUTONDE.ifite(list, value) -> bool
KIN_URUTONDE.kora_ijambo(list) -> string

KIN_INYANDIKO.soma(path) -> string
KIN_INYANDIKO.andika(path, text) -> bool | string
KIN_INYANDIKO.vugurura(path, text) -> bool | string
KIN_INYANDIKO.siba(path) -> bool | string


KIN_URUBUGA.saba(url [, method [, body [, headers]]]) -> object | string
  # success object: { kode: number, umubiri: string, imitwe: object }
  # imitwe keys use underscores (content_type); redirects not followed
  # failure: error message string (e.g. Request timed out)

KIN_IGIHE.isaha() -> string
KIN_IGIHE.umunsi() -> string
KIN_IGIHE.itariki() -> string

KIN_IMIBARE.pi -> number
KIN_IMIBARE.umubare_utazwi(min, max) -> number
KIN_IMIBARE.kuraho_ibice(x) -> number
KIN_IMIBARE.umuzikare(x) -> number
KIN_IMIBARE.sin(radians) -> number
KIN_IMIBARE.cos(radians) -> number
KIN_IMIBARE.tan(radians) -> number
